<?xml version="1.0" encoding="utf-8"?>
<menu xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    xmlns:tools="http://schemas.android.com/tools">
    <item
        android:id="@+id/item_menu_edit_undo"
        android:enabled="false"
        android:icon="@drawable/ic_round_undo"
        android:title="@string/undo"
        app:showAsAction="always"
        tools:ignore="AlwaysShowAction" />

    <item
        android:id="@+id/item_menu_edit_redo"
        android:enabled="false"
        android:icon="@drawable/ic_round_redo"
        android:title="@string/redo"
        app:showAsAction="always" />

    <item
        android:id="@+id/item_menu_edit_preview"
        android:icon="@drawable/ic_round_preview"
        android:title="@string/preview"
        app:showAsAction="always" />

    <item
        android:id="@+id/item_menu_edit_save"
        android:enabled="false"
        android:icon="@drawable/ic_round_save"
        android:title="@string/save"
        app:showAsAction="always" />

</menu>